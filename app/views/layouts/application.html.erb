<!DOCTYPE html>
<html>
  <head>
    <title>RailsStockTradingApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
     <nav class="w-full h-10 bg-gray-700 flex p-2 text-white justify-between">
        <h1 class="font-extrabold">Bull&Bear</h1>
        <% if current_user %>
        <ul class="flex w-[30rem] justify-between">
          <li>Home</li>
          <li>Portfolio</li>
          <li>Transactions</li>
        </ul>
          <%=current_user.email%>
          <%= link_to "Logout", destroy_user_session_path, method: :delete %>
        <%end%>
     </nav>
      <p class="notice bg-slate-300"><%= notice %></p> 
      <p class="alert bg-slate-300"><%= alert %></p> 

    <main class="container mx-auto mt-28 px-5">
      <%= yield %>
    </main>
  </body>
</html>
